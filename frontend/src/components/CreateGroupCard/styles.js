import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    paper: {
        width: "100%",
        padding: 20,
        borderRadius: 10,
        margin: "auto",
        marginTop: 50,
    },
    inputContainer: {
        display: "flex",
        flexDirection: "column",
        width: "60%",
        margin: "auto",
    },
});