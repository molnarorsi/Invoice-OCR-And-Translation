import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    rootContainer: {
        textAlign: "center",
        color: "black",
    },
    table: {
        marginTop: "20px",
        width: "80%",
        margin: "auto",
        height: "100%",
    }
});