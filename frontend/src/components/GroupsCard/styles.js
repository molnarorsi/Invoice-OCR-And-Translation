import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    card: {
        width: 300,
        margin: 'auto',
        textAlign: 'center',
        marginTop: 50,
        backgroundColor: '#d6536d',
    },
});