import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    chartContainer: {
        display: 'flex',
        justifyContent: 'space-between',
        flexWrap: 'wrap',
    },
});